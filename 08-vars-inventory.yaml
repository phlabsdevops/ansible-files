#Invenotory level variables
- name: variables from Incventory level
  hosts: localhost
  tasks:
  - name: check variables
    ansible.builtin.debug:
     msg: "Hi, I am learning {{Course}}. my trainer is {{Trainer}} and duration is {{Duration}}"